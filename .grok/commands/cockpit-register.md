---
description: Interactive Grok Build session — dump full 08 risk register here. GO writes, SAVE DRAFT stays in Grok, EDIT revises with no write.
argument-hint: "[desk] [--session]"
---

Parse `$ARGUMENTS`: desk; optional `--session` (default when OPEN GROK from Risks).

**Surface:** this Grok Build chat. **GO writes.** Glass ACCEPT of chips is the alternate commit.

Decision-support only. Never silent-write `08-risks-*.md` or `ontology/store/`.

**House CONFIRMED first.** If FORMING, STOP and run `/cockpit-propose {desk} --session`. After house GO, stay in this chat.

## Dump

Load this desk’s house + `08` + pack. **Align register** to that house (ask; no silent-delete). Dump the **full `08`** in one fence. Keep `### Rn`, Status / Grade / tripwire tables.

Then:

**GO** (writes register) · **SAVE DRAFT** (Grok only, no glass chip) · **EDIT** (change it here, no write)

Wait. Until GO, do not `propose_*`. After GO: `propose_add_risk` / `propose_risk_status` / `propose_risk_tripwires` then `commit_on_go` kind=register. `register-closeout.mjs --slug {desk}` must PASS.

Do **not** auto-propose house from a register edit. **Ask** only if a new/changed Rn **contradicts** the stance, is a **flip trigger** the house lacks, or they say **“this is now on the house.”** If yes → house dump, they GO `kind=house`.

GO aliases: looks good, that’s my house, CONFIRM, ACCEPT REGISTER.

Glass viewer: `#/{desk}/risks`.
