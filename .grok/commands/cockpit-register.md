---
description: Interactive Grok Build session — dump full 08 here. GO writes, SAVE DRAFT parks, EDIT revises with no write.
argument-hint: "[desk] [--session]"
---

Parse `$ARGUMENTS`: desk; optional `--session` (default when OPEN GROK from Risks).

**Surface:** this Grok Build chat. **GO writes.** Glass ACCEPT chips are the alternate commit.

Decision-support only. Never silent-write `08-risks-*.md` or `ontology/store/`.

**House CONFIRMED first** (live file, after GO + `commit_on_go` on house). If FORMING, STOP and run `/cockpit-propose {desk} --session`. After new-desk house GO, **stay in this chat**.

## Three words

- **GO** (aliases: looks good, ACCEPT REGISTER, CONFIRM) — propose chips matching the dump they just saw, then **`commit_on_go` kind=register**.
- **SAVE DRAFT** — pending chips only. Do not `commit_on_go`.
- **EDIT** (aliases: change / fix / drop Rn / add / not yet) — **no propose, no write.** Apply delta, dump full `08` again, wait.
- **STOP** — no proposal.

Prompt under every fence:

**GO** (writes register) · **SAVE DRAFT** (pending chips) · **EDIT** (change Rn here, no write)

## Session

1. `list_desks` — pin_ok or STOP.
2. Load CONFIRMED house + `08-risks-*.md` + pack/SoR.
3. **Align to that house** (ask; no silent-delete): Rn the stance lives on; drop/add only if they say so.
4. **Dump the full proposed `08`** in one fence. Entire file.
5. They say **GO**, **SAVE DRAFT**, or **EDIT**. Until GO or SAVE DRAFT, do not `propose_*`. EDIT → dump again.
6. After **GO**: `propose_add_risk` / `propose_risk_status` / `propose_risk_tripwires` matching that dump. Print ids. **`commit_on_go` kind=register**. Do not wait for glass ACCEPT.
7. `node scripts/register-closeout.mjs --slug {desk}` must **PASS**. COMPILE BOOK if pack lags.

Cap: ≤4 web searches per new risk.
