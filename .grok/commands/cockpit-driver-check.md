---
description: Check one kept engine — print, news, or on-demand. GO appends a log line. No house write.
argument-hint: "[desk] [driver]"
---

MCP `get_driver_sor`. Research **this engine only** against its **House:** cite and **Watching:** line.

Return one log line: date (`YYYY-MM-DD`), via (`print` | `news` | `open` | `note` | `house`), fact.

When they agree, `propose_driver_log` with **driver** (D1 or the title), **date**, **via**, **fact**. Optional **figures** if a print replaced the numbers. Optional **open** if a new question showed up.

They **GO** `commit_on_go` kind=drivers. That appends the line to 09. It does not write 08. It does not rewrite the house.

No status. Do not say ON-PLAN, TRACK, or OFF-PLAN.

If the finding **contradicts** the house story, **ask** — house GO or drop the driver. Do not silent-write either file.
