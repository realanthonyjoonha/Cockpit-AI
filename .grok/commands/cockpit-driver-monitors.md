---
description: Add one still-open question on a kept driver. GO appends it to 09. Not a risk tripwire.
argument-hint: "[desk] [driver]"
---

This is not a monitor table and not the risk register.

Draft **one** question that is still open on this driver. The user keeps or drops it.

When they keep it, `propose_driver_log` with **driver** and **open** (the question). No fact required. They **GO** `kind=drivers`. That appends the question under Still open. It does not write 08 or the house.

No status. Not tripwires.
