---
description: Company Background product-map + HTML architecture + Study Tree deepen (harvest → map → spine; on-demand deep-dives). Not thesis, not House, not Model.
argument-hint: "[desk] [primer|tutor|deepen] [node_id]"
---

Parse `$ARGUMENTS`: **desk** (slug/ticker) + optional **primer** | **tutor** | **background** | **deepen** + node id.

Load **`.grok/skills/company-learn/SKILL.md`** and execute it.

Depth bar: primer ≥1400w · ≥9 ready nodes · ≥9 lessons ≥450w · ≥4 source pins · ≥3 diagrams. `fabless_platform` needs mechanism + process nodes and all four diagram types. `segments` may set `group` / `parent`; tile size never encodes $. Glass **HTML** from `product-map.json` diagrams[] — no mermaid, no SVG, no per-ticker art.

Deepen: one map node → product-mechanism deep-dive (setup · mechanism · evidence · what a figure is not · GAP · exec). Reports *craft*, not thesis lane (no house/register/propose_*). Pace through. POST `/api/{slug}/learn/deep`.

Photos (enrichment, not a gate): primary = IR/newsroom/SEC allowlist; tape = engine-built Google Images queries. Identification gate or GAP. Server downloads bytes. Tape is **web, not a filing** and mints nothing. No GenerateImage / no SERP scrape / no Google API key.

If desk missing, `list_desks` then ask once.

Default mode: **tutor** when a primer already exists on glass; **primer** when EMPTY.

Decision-support only. No buy/sell/PT. Never write house/risks/pack.
