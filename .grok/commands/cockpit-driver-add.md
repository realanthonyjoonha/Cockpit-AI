---
description: Pin a named business engine (AWS, neoclouds, ads mix). house_cite required.
argument-hint: "[desk] [engine]"
---

Parse `$ARGUMENTS`: desk + the engine they want watched closer (their words).

Load CONFIRMED house. The engine must already be a **claim in the house**. If not: STOP — GO house first.

Do **not** use a house heading as the driver title. No status.

`propose_keep_driver` with **title** = the engine name, **house_cite** = a quote that appears in the house, **watching** = what they want followed. Optional **why**. Then glass ACCEPT or GO `kind=drivers`. Never write 08. Never write the house. Never auto-keep.

Cadence later: print, news, or on-demand. That appends a log line. It does not change this pin.
