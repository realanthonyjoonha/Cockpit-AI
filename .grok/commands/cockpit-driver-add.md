---
description: Pin a named business engine (AWS, neoclouds, ads mix). house_cite required.
argument-hint: "[desk] [engine]"
---

Parse `$ARGUMENTS`: desk + the engine they want watched closer (their words).

Load CONFIRMED house. Use **their words** as the title. Do **not** replace their name with a house heading.

If that side is already a claim in the house: pin it. Do **not** edit the house. `propose_keep_driver` with **title** = their name, **house_cite** = a quote that appears in the house, **watching** = what they want followed. Then glass ACCEPT or GO `kind=drivers`. Never write 08.

If it is not in the house: do **not** pin it and do **not** rewrite the house. Ask once — house GO first, or drop the driver. No status. Never auto-keep.

Cadence later: print, news, or on-demand. That appends a log line. It does not change this pin.
