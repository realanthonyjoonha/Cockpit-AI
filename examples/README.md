# Examples (optional)

**Cold start is empty.** Examples are **not** part of cold-start success.

| Path | Role |
|------|------|
| [`microsoft/`](./microsoft/) | Optional thin-desk **reference** (files live in the live monorepo; install into a kernel) |

## Product layers

```text
1. Kernel (empty cold start)     scripts/export-kernel.sh → DEST
2. Optional MSFT example         scripts/install-example-msft.sh DEST
3. Live monorepo                 this repo with your real books
```

Never invent research. User owns house/risk ACCEPT after underwriting.
