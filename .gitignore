# Dependencies / build
node_modules/
dist/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Secrets — never commit
.access.json
.session-secret
**/.session-secret
**/.access.json
.env
.env.*
*.pem
memory-cockpit-v2/.session-secret

# Local company books if someone scaffolds by mistake before push (keep product empty)
# (Do not force-ignore raw/ entirely — empty scaffold dirs may exist)
# Privacy: never commit Anthony/friend research books into product SoR
research-wiki/house-view-*.md
research-wiki/raw/*
!research-wiki/raw/.gitkeep
research-wiki/wiki/entities/**
research-wiki/wiki/sources/**
research-wiki/cockpit/street/*.json
research-wiki/cockpit/street/history/**
research-wiki/cockpit/briefs/**
research-wiki/cockpit/proposals/**
ontology/packs/*.json
!ontology/packs/.gitkeep
ontology/store/by_ticker/*.json
!ontology/store/by_ticker/.gitkeep
memory-cockpit-v2/config/thin-desks.json.bak*

# Friend/local dogfood leftovers
cockpit-kernel-fresh/

# Machine-local Grok MCP pin (absolute paths; rewritten by install-grok-mcp / OPEN GROK)
.grok/config.toml

# OS / editor
.DS_Store
.obsidian/
*.swp
*~

# Logs & local runtime
*.log
logs/
cockpit/.sync-state.json
cockpit/.sync.lock
memory-cockpit-v2/logs/

# Optional local noise
.DS_Store
