---
description: Cockpit Cursor e2e — isolated testing cockpit, not live kernel books
alwaysApply: true
---

# Cursor e2e (testing cockpit)

**Repos:** Project root = private `realanthonyjoonha/cockpit` (this clone). Books source = private `realanthonyjoonha/cockpit-vault` — on Cursor cloud **`/home/ubuntu/cockpit-vault`**, on a Mac `~/Desktop/cockpit-vault`, or `COCKPIT_VAULT`. Read to copy into the seal; never write; never MCP-pin the vault. Not Cockpit-AI. Not cockpit-personal.

**Three-desk lab (NVDA, MU, LLY):**

```bash
./scripts/dogfood-e2e.sh --slugs nvda,mu,lly
```

Copies those desks from the vault into gitignored `$PWD/.cockpit-dogfood`. MCP `cockpit-research-dogfood`. Glass `:4695`. If vault is missing, fail closed.

Playbook (includes paste block): `docs/CURSOR-E2E.md`.

**Stop if** `list_desks` includes nbis/avgo or `monorepo_root` is cockpit-vault / live kernel wiki. Product `desks` must stay `[]`.

Decision-support only. No git push unless the human said **push**.
